import {RequirementOption} from "../../data/CSProgram";
import {CourseID} from "../../data/CourseID"

let temp = new CourseID('CS', 321);

test("Test if the user has entered correct course format", () => {
    expect(temp.toString()).toEqual();
}